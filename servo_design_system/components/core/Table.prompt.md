# Table

Data table for queues and logs. Header is mono uppercase; rows highlight on hover.

```jsx
<Table columns={cols} rows={tickets} onRowClick={open} />
```
Mark id/count columns `mono` so they render in IBM Plex Mono.
