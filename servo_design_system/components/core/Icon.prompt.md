# Icon

Lucide glyph wrapper — the only sanctioned icon source in Servo.

```jsx
<Icon name="shield-check" size={16} />
```
Needs the Lucide UMD script on the page. 16px at UI scale, 14px inside buttons/badges, stroke 2.
