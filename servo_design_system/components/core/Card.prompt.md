# Card

The standard panel: hairline border, 10px radius, inset top highlight.

```jsx
<Card title="Open load by category" padded={false}><Table … /></Card>
```
Use `padded={false}` when a table or chart should bleed to the edges; `footer` for action rows.
