# CommandPalette

Ctrl/⌘K search — tickets by number or text, plus page jumps.

```jsx
<CommandPalette query={q} onQueryChange={setQ} groups={[{label:"Tickets",items:[{number:1061,label:"Star on GitHub button is unreadable"}]}]} />
```
