# Hygiene audit — 2026-09-03 (hyg-audit-07)

Generated by `node scripts/repo-refs.mjs --evidence` and `npm run claims:audit`,
per the item's acceptance. First audit on this calendar day, so no `-N` suffix —
the same-day convention [audit-2026-08-30-2.md](audit-2026-08-30-2.md) set
applies only where a day carries more than one.

No `-evidence.md` companion is committed. `hyg-audit-05` stopped committing one
and recorded why as question 75; this tick follows that precedent and generated
its evidence to a path outside the repository.

This tick ran in the ORDINARY pick order, not by a cadence jump. It deleted
nothing, moved nothing, appended no backlog content beyond its own re-arm, and
added no baseline row — nothing qualified.

## The pick chain

Shorter than any previous audit's, because the backlog has emptied. Re-derived
from `origin/main` at `d64e1c3` and from GitHub, not inherited:

- **102 backlog items carry a status; 101 read `done`.** The only `todo` item in
  the whole file is `hyg-audit-07` (block header at spec.md:2328). The two other
  `status: todo` lines, at spec.md:48 and spec.md:394, belong to the format
  TEMPLATE blocks headed at :47 and :393, which carry the literal id `<id>` and
  are not items.
- **Its dependencies are satisfied**: `hyg-01` (`done` 2026-08-27) and `hyg-04`
  (`done` 2026-08-29).
- **Nothing is `doing`, `blocked` or `review`.** No item carries those statuses,
  and the GitHub API's open-pull-request list for `ricauts/servo` is empty, so
  the review slot is free and nothing is waiting in it.
- So §0.2 step 4 selects `hyg-audit-07`, and there is no second candidate to
  reason about. The anti-stall provisions of §0.6 are all inert this tick.

**The cadence case, which the item's fourth criterion requires the commit message
to state: NOT DUE, and the tick ran in the ORDINARY pick order anyway.** §15
holds 148 rows; the most recent `hyg-audit-*` row is `hyg-audit-06`, and **12**
rows have accrued since it against a threshold of twenty. The second half of the
cadence test — no item in `review` — is satisfied, which it was not at
`hyg-audit-06` (PR #27 was open then), but the row count is not met, so the item
is not DUE. It did not need to be: with
every other item `done`, the ordinary pick order reaches it anyway. No jump was
taken and none was available, since the §0.2 step 4 sentence that would permit
one is owner-applied and still absent (question 34c).

**Landing rail check, per the owner answer of 2026-08-28 recorded under question
49:** `main`'s latest CI run before this tick is run 241 (`33682663509`, head
`d64e1c3`), concluded **success**. The rail that forbids merging a Tier-A branch
onto a red `main` is satisfied.

## Verdicts

Both scanners are clean on the base tree.

**Reference scan (`repo-refs --check`): clean.**
775 tracked files scanned on the base tree. 17 findings — 13 unreferenced files
and 4 package findings — every one covered by a row of
`tests/fixtures/repo-refs-baseline.json`, **0 uncovered**. 211 unreferenced files
sit on the never-delete list. 29 unreferenced exports (reported, not a `--check`
finding).

**Claims audit (`claims:audit`): clean on both halves.**
Phrases: 18 files, 10 banned, 5 allowed, 11 exempt, **0 violations**. Dead paths:
43 files, 6470 path references checked, 6203 resolved, 267 exempt, 138 skipped as
not repo-relative, 0 outside the repository, **0 missing**.

With this tick's own writes in scope — this report and the spec.md edits — the
same two scans read: phrases unchanged at 18 / 10 / 5 / 11 / **0**; dead paths
44 files, 6484 references, 6217 resolved, 267 exempt, 141 skipped, still **0
missing**. `repo-refs --check` still reports 17 findings, 0 uncovered and 29
exports, but **two of its counts move by one and both readings are published
here**: the scanned-file count reads 775 on the base tree and **776** once this
report is tracked, and the never-delete list reads 211 and **212**, because this
report is itself an unreferenced file that the `docs/hygiene/` prefix rule keeps.
The scan set is `git ls-files`, so both move together on staging. *One thing that
scan caught and this tick had to work around: naming `shoot-og.mjs` and
`run-relay.ts` at their pre-`hyg-09` root paths made the dead-path scan fail with
two missing paths. The `hyg-05` and 09-02 evidence reports solved that with a
canon exemption in `docs/POSITIONING.md`, which this item may not write — its
first criterion confines it to this report, baseline rows and spec.md — so the
report names those two by basename instead. Worth knowing before the next audit
writes a sentence about a deleted path.*

`npm run typecheck` exits 0. `npm test` is green: **102 test files, 1373 tests,
0 failures**, run against a real `pgvector/pgvector:pg17` container on this
runner. The other guards — `spec-lint` (102 items on the base tree, 103 once
this tick's re-arm is in), `landing-tier` (A), `policy-guard` (30 default rows,
baseline in sync), `approval-path-guard` (approval path unchanged,
`c8556ef07f020e45`) — all pass on both trees.

*Runner note, recorded because it cost this tick twenty minutes and will cost the
next one the same:* the image pull denial of question 40 still stands verbatim —
`production.cloudfront.docker.com` answers 403 to CONNECT — and question 44's
`mirror.gcr.io` workaround is still what gets the images. The pgvector manifest
digest served by the mirror today is
`sha256:cf134a767f474095eeba57e0117be8e568e011a63f33fbf252f14c9b760f8e6f`,
byte-for-byte the digest question 44 recorded from Docker Hub's own manifest, so
it is the same image and not a substitute. One detail question 44 did not record
and this tick had to find the hard way: the stack must be brought up under the
compose project name **`servo-test`**, because `tests/backup-restore.test.ts:45`
pins `-p servo-test` on its `docker compose exec` fallback. Brought up under the
default project name (`servo`, from the directory) every other test passes and
that one fails with `service "db" is not running` — which is question 44's own
finding (1), the project-name collision, biting from the other direction.

## What changed since hyg-audit-06

**A great deal — the first audit in six to face real movement.** `git diff
0be87cf..d64e1c3` is 110 files, +3190/−418, across 35 commits and 18 merges;
with this tick's own commit in scope the same range reads 111 files, +3445/−418
and 36 commits, the merge count unchanged. At
`0be87cf` nine item blocks were not `done` — `hyg-05`, `hyg-06`, `hyg-07`,
`hyg-09` and `dcl-08` `todo`, `ext-05`, `dcl-07` and `fed-06` `blocked`, plus
`hyg-audit-07` itself. All eight of the others landed in the window, leaving
this one.

Scanner movements, all explained by that work:

| | hyg-audit-06 | this tick | cause |
|---|---|---|---|
| files scanned | 767 | 775 | net of `hyg-05`'s deletions and the new tests/docs |
| `--check` findings | 20 (15 files + 5 packages) | 17 (13 + 4) | `hyg-05` removed `gifenc` and the dead files; `hyg-09` folded `shoot-og.mjs` and `run-relay.ts` out of the keep-list |
| never-delete list | 215 | 211 | same |
| unreferenced exports | 37 | 29 | `hyg-05`'s deletions and `src/lib/utils.ts` (−33) |
| claims paths checked | 5220 | 6470 | the new evidence report, the docling docs, `THIRD_PARTY.md` (+46) |
| claims paths exempt | 32 | 267 | the canon commit `a023ec3` exempting the 09-02 evidence reports that name `hyg-05`'s deleted paths |

`tests/fixtures/repo-refs-baseline.json` moved too, and the exact shape of that
move matters to question 101 below. **Four** path rows were repointed, all of
them into `scripts/media/` — `make-before-after.mjs`, `record-approval.mjs`,
`screenshot.mjs` and `readme-screenshots.mjs`; the string `scripts/dev` does not
appear in the keep-list at all. **Three** rows were removed: `shoot-og.mjs` and
`run-relay.ts`, at the root paths they held before `hyg-09` moved them, and the
`sharp` package row. A fourth, `gifenc`, was removed and re-added in the same
commit with a different `finding` and reason, which is a rewrite rather than a
removal. Each removal rode **in the commit that ended it**, which is the removal
rule working as written. *The `sharp` removal is the one to notice: it was
dropped on the premise that the media-tooling block in `docs/MEDIA-GUIDE.md`
covers it, while `ffmpeg-static`'s row was kept on the same premise — and
question 101 below shows that premise is false, which is why the kept row turns
out to be load-bearing.* (Naming the two removed script rows at their old root
paths in this report is what
`claims:audit`'s dead-path scan is for, so they are named by basename here; the
`hyg-05` and 09-02 evidence reports needed a canon exemption for exactly this,
and an audit tick may not write one, since its first criterion confines it to
this file, baseline rows and spec.md.) **This tick added, removed and edited no
baseline row** — every one of the 17 findings is already covered, and the one
file this tick creates (`docs/hygiene/audit-2026-09-03.md`) lands on the
never-delete list under the `docs/hygiene/` prefix rule
(`scripts/repo-refs.mjs:123`), so it is not a finding and needs no row.

## Findings

Both scanners being clean is why this tick spent its capacity elsewhere. Six
independent lenses were run over surfaces neither scanner reads — spec.md's
bookkeeping against the tree, the keep-list and baseline against reality, the
110-file diff read adversarially, the audit trail's own past claims, the
documentation surface and CI, and the guard scripts hyg-audit-06 did not
audit — producing **32 candidate findings**. Twenty-four of those were handed to
an independent adversarial verifier that had not seen the finder's reasoning,
told to reproduce the evidence first-hand and to reject anything already recorded
under §14, anything deliberate-and-documented, and anything cosmetic.
**Fifteen survived**; nine were refuted or found already recorded. The eight
candidates beyond the verification cap were not verified, and are excluded here
rather than reported unchecked — the same rule `hyg-audit-06` applied to its own
five.

The fifteen are consolidated into **questions 99 to 106** under §14, each with a
keep assumption. Nothing was fixed: this item deletes nothing, moves nothing and
writes no backlog item. In outline:

- **99** — the two headline bookkeeping figures are both wrong (98 vs 102), and
  "the backlog is complete" is a state the re-arm criterion makes unreachable.
- **100** — the keep-list rows naming a `done` owner have gone from four to nine,
  and q72's one-file remedy no longer suffices.
- **101, 102** — the media allowlist is inert in both its readers, and the
  scanner that reads it breaks its own purity contract and `hyg-04`'s acceptance;
  `hygiene:check` dies on ENOENT if `docs/MEDIA-GUIDE.md` is absent.
- **103** — the design-system readme §0.5 makes a mandatory read still points UI
  ticks at a directory `hyg-06` renamed, and three guards each miss it for a
  different structural reason.
- **104** — four questions sit below the `## 15.` heading, one splitting the
  changelog table; a live test's §14 slice cannot see them; and one citation
  names a question that was never written.
- **105** — seven questions still read as live instructions to merge PRs that
  merged days ago, four more have expired premises, and q88's count is now
  sixteen rather than twelve.
- **106** — README.md and ROADMAP.md file shipped capability as roadmap — the
  claims rule failing in the safe direction, and outside q85's scope.

Two findings are worth separating from the rest because they are executable
defects rather than drift: **102** (`hygiene:check` exits 1 on an unhandled
ENOENT, proved against a mirror of the repository with one file removed) and
**101** (removing the `ffmpeg-static` keep-list row produces a violation, which
disproves that row's own stated reason). Both were introduced by `hyg-09` on
2026-09-02 and neither is visible to any scanner in the tree.

## Method note

Neither scanner found any of the fifteen. That is the same conclusion
`hyg-audit-06` reached and question 78 predicted: a stale citation, a false
prose reason, a question whose premise expired, and a number in one section
contradicting a count in another are all invisible to a reference graph and to a
phrase linter. Thirty agents, 805 tool calls; every reported finding was
reproduced first-hand by an agent that had not written it.

One methodological correction worth recording, because a verifier hit it and a
later tick will too: counting "changelog rows since the most recent `hyg-audit-*`
row" by grepping row text gives **9**, because one later row — the `hyg-05
(landed)` row — mentions `hyg-audit-` only in its prose and is mistaken for the
anchor. Counting by the row's item-id column gives **12**, which is what the
cadence clause means. This tick used 12.

Two disclosures this report owes about its own effect on the tree, both of them
instances of findings it records. **This tick's changelog row lands inside the
broken table fragment question 104 documents** — the blank lines around the
misfiled question 94 close the changelog table, so the rows after it render as a
paragraph of literal pipe characters; there were five, and appending is the only
thing an append-only changelog permits, so after this commit there are six. And
**question 104's line numbers are measured against `d64e1c3` and shift the moment
this commit lands** — by +18 in §14, +34 below `## 15.`, and +35 below this
tick's own changelog row, which is where three of the four questions it names
sit. The question is labelled `d64e1c3`-relative and publishes both readings for
that reason. An audit whose thesis is that stale citations
are invisible to both scanners should say plainly that it has written some.
