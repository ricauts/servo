# Hygiene audit — 2026-09-02 (hyg-audit-04)

Generated by `node scripts/repo-refs.mjs --evidence docs/hygiene/audit-2026-09-02-2-evidence.md`
and `npm run claims:audit`, per the item's acceptance. The `-2` suffix follows
the precedent [audit-2026-08-30-2.md](audit-2026-08-30-2.md) set when
`hyg-audit-02` ran on the same calendar day as `hyg-audit-01`; this audit runs
on the same day as [audit-2026-09-02.md](audit-2026-09-02.md).

This tick ran in the ORDINARY pick order, not by a cadence jump, and the pick
chain was re-derived from the tree rather than inherited: `hyg-05` holds the
review slot (PR #27, open and unmerged since 2026-09-01T09:50Z, so §0.3's state
table makes it `review` however `main`'s block reads), `hyg-06` is
dependency-blocked on it, `hyg-07` and `hyg-09` are Tier C and each would be
§0.6's forbidden second open Tier-C PR, `dcl-07` is `blocked` and `dcl-08`
depends on it. This tick deleted nothing, moved nothing, appended no backlog
content beyond its own re-arm, and added no baseline rows — nothing qualified.

## Verdicts

**Reference scan (`repo-refs --check`): clean.**
766 tracked files scanned (764 at the last audit; the count includes this
report and its evidence file, which were staged before the final run so the
numbers describe the tree as it is committed). 20 findings, all covered by
`tests/fixtures/repo-refs-baseline.json`, 0 uncovered — and, in the other
direction, 0 baseline rows without a matching finding. 214 unreferenced files
sit on the never-delete list, this report among them. Full evidence:
[audit-2026-09-02-2-evidence.md](audit-2026-09-02-2-evidence.md).

**Claims audit (`claims:audit`): clean on both halves.**
Phrases: 17 files, 10 banned, 5 allowed, 11 exempt, 0 violations. Dead paths:
39 files, 5206 references checked, 5174 resolved, 32 exempt, 120 skipped as not
repo-relative, 0 outside the repository, **0 missing**. The reference count is
up ~1,250 on the last audit purely because this tick's own two reports joined
the scan; the two files scanned are those same two.

## What changed since hyg-audit-03 (2026-09-02, earlier the same day)

**Nothing.** This is the finding, and it is the reason the tick is worth
recording rather than a reason to hide it. `git diff` between `hyg-audit-03`'s
work commit and the `origin/main` this branch was cut from is EMPTY: the tree
this audit scanned is byte-identical to the tree the previous audit scanned,
because the only commits in between are that audit's own work commit and its
merge. Every number below is therefore a restatement, not an observation.

**Findings: 20 → 20.** The same twenty, all baselined, none stale.

**Two files became unreferenced and neither needs a row**, because both are on
the never-delete list: this tick's own two reports under `docs/hygiene/`. That
is the same mechanism `hyg-audit-03` recorded for its own pair, one audit
earlier — an audit's reports are the only thing an audit reliably adds to the
unreferenced set.

**Unreferenced exports: 37 → 37**, the same thirty-seven, none in and none out.
An unreferenced export is not a `--check` finding and needs no baseline row.

**Canon movements: none.** The transitional `paths-exempt` groups remain the
two `hyg-audit-03` left (`dcl-07`, `hyg-09`).

## Open questions for the owner

One raised, recorded rather than acted on — this tick removes nothing and edits
no baseline row. Questions 72 and 73, raised by `hyg-audit-03`, are unchanged
and still stand. See §14 of `spec.md`:

- **Question 74 — the audit re-arms faster than its own cadence allows, so
  with the backlog stalled it becomes the only thing the loop can do.** The
  item's cadence criterion says it is DUE at twenty or more changelog rows
  since the last `hyg-audit-*` row AND no item in review. **Zero** rows have
  accrued since the previous audit's row and PR #27 is open, so both halves
  fail — yet the item is still the first thing §0.2 step 4 reaches, because
  every item ahead of it is in review, dependency-blocked on a review item, or
  Tier C behind the one-review-slot rule. Completing it re-arms the next one at
  the end of the backlog, which reproduces the same state on the next tick.
  *Keep assumption: the item ran, because its own criteria say a clean audit is
  still a completed tick and name the ordinary pick order as the no-jump
  fallback. The loop APPLIED no fix — question 74 names three, and all three
  are the owner's to make — and it made no protocol edit.*

## Cadence

Changelog rows since the `hyg-audit-03` row: **0** — that row is the last line
of the section 15 table on `origin/main`, and this branch adds the next one.
The twenty-row half of the cadence is **not** satisfied, and neither is the
other half (*and no item is in review*), since `hyg-05`'s PR #27 has been open
since 2026-09-01. The audit is therefore **not DUE on either half** — a
stronger statement than the previous audit could make, which cleared the
twenty-row half at 37 rows and failed only the review half. It ran because it
was the next item in the ORDINARY pick order. That is the acceptance
criterion's own no-jump case, and the commit message states it the same way.
The gap between "not due on either half" and "runs anyway" is question 74.

## What this tick did not do

Deleted nothing. Moved nothing. Removed no baseline row and edited none. Added
no baseline row — nothing qualified. Appended no backlog item except the
re-arm the acceptance requires. Proposed no deletion in prose. Made no edit to
the tick protocol in §0, and did not flip `hyg-05`'s status on `main`, which
question 55 classes as a protocol edit and question 34 reserves to the owner.
