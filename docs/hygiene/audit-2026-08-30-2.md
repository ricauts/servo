# Hygiene audit — 2026-08-30, second run (hyg-audit-02)

Generated by `node scripts/repo-refs.mjs --evidence docs/hygiene/audit-2026-08-30-2-evidence.md`
and `npm run claims:audit`, per the item's acceptance. Same-day as the
first audit (hyg-audit-01): this tick ran in the ORDINARY pick order — the
owner applied no jump, and with every remaining non-Tier-C item exhausted,
the audit was the pick. This tick deleted nothing, moved nothing, appended
no backlog content, and added no baseline rows — nothing qualified.

## Verdicts

**Reference scan (`repo-refs --check`): clean.**
21 findings, all covered by `tests/fixtures/repo-refs-baseline.json`,
0 uncovered. One finding FEWER than the morning run: the dcl-02/dcl-03
design-doc retargets resolved a reference the baseline had carried. Full
evidence: [audit-2026-08-30-2-evidence.md](audit-2026-08-30-2-evidence.md).

**Claims audit (`claims:audit`): clean on both halves.**
Phrases: 12 files, 10 banned, 5 allowed, 11 exempt, 0 violations. Dead
paths: 29 files, 2677 references checked, 2640 resolved, 37 exempt, 100
skipped as not repo-relative, 0 outside the repository, 0 missing. The
scan has grown by more than a thousand references since the morning run —
the second evidence file itself — every one resolving.

**Canon movements since the morning audit (forced, not elective):** none.
The transitional exemption groups now number three (dcl-06, dcl-07,
hyg-09) — dcl-09's deliverables retired nothing because its paths were
never forward-referenced.

## Open questions for the owner

None raised by this audit — there is no proposed removal to carry a keep
assumption. The standing owner items are unchanged and remain the
blockers for the rest of the backlog: **PR #19 (fed-05) holds the Tier-C
review slot** with fed-06 queued behind it (q51); **question 56** (six
laundering/injection paths in the shipped cnp-02) is open; and dcl-06,
ext-01, hyg-05, hyg-07 and hyg-09 all queue on the one slot.

## Cadence

Rows since the hyg-audit-01 row: 9 (dcl-02 correction excluded by none —
counted as written: db-10-correction, kb-02-verification, hyg-08,
hyg-audit-01, dcl-02, dcl-03, dcl-04, dcl-05, dcl-09, cnp-03 land in
order, several with same-day rows). Under the twenty-row rule the audit
is NOT due; it ran because it was the next item in the ORDINARY pick
order — the acceptance's exact slower-cadence case, stated here as the
commit message also states it.
