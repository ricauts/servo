# Hygiene audit — 2026-09-03 (hyg-audit-10)

Generated by `node scripts/repo-refs.mjs --evidence` and `npm run claims:audit`,
per the item's acceptance. Fourth audit on this calendar day, so the `-4` suffix
continues the convention [audit-2026-08-30-2.md](audit-2026-08-30-2.md) set:
`hyg-audit-07`'s report is [audit-2026-09-03.md](audit-2026-09-03.md),
`hyg-audit-08`'s is [audit-2026-09-03-2.md](audit-2026-09-03-2.md) and
`hyg-audit-09`'s is [audit-2026-09-03-3.md](audit-2026-09-03-3.md); none is
overwritten.

No `-evidence.md` companion is committed. `hyg-audit-05` stopped committing one
and recorded why as question 75; this tick follows that precedent and generated
its evidence to a path outside the repository.

This tick ran in the ORDINARY pick order, not by a cadence jump. It deleted
nothing, moved nothing, appended no backlog content beyond its own re-arm, and
added no baseline row — nothing qualified.

**Both scanners are clean, and this tick spent its capacity on what they cannot
read.** Six lenses, one per surface no scanner in this repository inspects, each
finding put to an adversarial refuter that had not seen the finder's reasoning,
then every survivor re-verified by hand before publication. Twenty candidates
went in; eighteen were confirmed, and they are written up as questions 124 to
130 — one confirmed candidate (the Docling configuration surface) stated as two
sub-points, so the seven questions carry nineteen lettered-or-standalone
entries. One of them is a security finding: **with `AUTH_SECRET` unset, Servo
signs its session cookies with a constant committed to this repository, and
nothing refuses to boot.**

## The pick chain

Re-derived from `origin/main` at `8b5e7ba` and from the GitHub API, not inherited
from the previous report.

**Every `spec.md` line number in this report is measured against `8b5e7ba`, the
tree the tick read, and every one of them shifts when this tick's own diff
lands.** This is question 104's convention, adopted here for the same reason it
was adopted there: `hyg-audit-08` published nine pre-insert coordinates and had
to correct them in a follow-up commit, and `hyg-audit-09` caught the same class
of error in its own report before landing. Read the coordinates against
`8b5e7ba`, or by the quoted text.

- **105 backlog items carry a status; 104 read `done`**, and the only `todo` item
  in the whole file is `hyg-audit-10` (block header at spec.md:2382, `status:` at
  :2383). On the landed tree those figures become 106 and 105, and the only
  `todo` becomes `hyg-audit-11`, because this tick flips `hyg-audit-10` to `done`
  and appends its successor. The two other `status: todo` lines, at spec.md:48
  and spec.md:394, belong to the format TEMPLATE blocks and carry the literal id
  `<id>`; they are not items. `node scripts/spec-lint.mjs` prints
  `OK (105 items)`, which is the same 104 + 1.
- **Its dependencies are satisfied**: `hyg-01` (`done` 2026-08-27, header at
  spec.md:1600, `date:` at :1602) and `hyg-04` (`done` 2026-08-29, header at
  spec.md:1659, `date:` at :1661). Its own `depends-on` line is spec.md:2387.
- **Nothing is `doing`, `blocked` or `review`.**
  `grep -cE "^status: (review|doing|blocked)" spec.md` returns `0`, and the
  GitHub API's open-pull-request list for `ricauts/servo` is empty, so the review
  slot is free and nothing is waiting in it.
- So §0.2 step 4 selects `hyg-audit-10`, and there is no second candidate. The
  anti-stall provisions of §0.6 are all inert this tick.

**The cadence case, which the item's fourth criterion requires the commit message
to state: NOT DUE, and the tick ran in the ORDINARY pick order anyway.** §15
holds 151 data rows (lines beginning `| 2026-`, the last at spec.md:3166). The
most recent row whose *item-id column* is a `hyg-audit-*` id is `hyg-audit-09`'s,
and it is that same last row — `awk 'NR>3166 && /^\| 2026-/' spec.md` returns
nothing — so **0** rows have accrued against a threshold of twenty. The second
half of the cadence test, no item in `review`, is satisfied. The row count is
not, so the item is not DUE; it did not need to be, because with every other item
`done` the ordinary pick order reaches it anyway. No jump was taken and none was
available: the §0.2 step 4 sentence that would permit one is owner-applied and
still absent (question 34c).

Counting by the item-id column is what the cadence clause means. A grep for the
string `hyg-audit` across §15 also matches spec.md:3144 and :3151, whose item ids
are `dcl-07 (blocked)` and `hyg-05 (landed)` and which merely mention an audit in
their prose — a distinction that would matter if the last row were one of those.
Here it is not: spec.md:3166 is both the last row in the file and tied for the
latest date, so NOT DUE is robust under file order and date order alike.

**Landing rail check, per the owner answer of 2026-08-28 recorded under question
49.** The rail: the loop refuses to merge a Tier-A/B branch while `main`'s own
latest CI run is failing. At the moment this tick started, `main`'s latest run was
**run 244 (`33742548432`, head `8b5e7ba`, attempt 1), concluded `success`** at
2026-09-03T10:11:47Z — the CI run of the merge commit `hyg-audit-09` created.
The rail is satisfied on the evidence, and unlike the previous tick this one had
nothing to re-run. Question 119's observation stands unchanged: the rail exists
only as prose, and nothing in `scripts/landing-tier.mjs` consults `main`'s CI.

**Adopt-first gate (§0.4):** nothing to adopt. This item writes one report, spec
prose and (conditionally) baseline rows; it adds no component, so the gate has
nothing to rule on. No dependency was added, removed or bumped, and
`package.json` is untouched. The gate did, separately, become the *subject* of
one lens this tick — see question 125 — but that is the ledger being audited, not
a new component being ruled on.

**Design-system read (§0.5):** not triggered as a UI tick — the diff is `docs/`
and `spec.md` only and changes no UI surface. The design system was nonetheless
*read* as an audit lens (`servo_design_system/SKILL.md`, `readme.md`,
`github.md`, `tokens/*.css`, the guideline cards), which is what produced
question 129.

## Verdicts

Both scanners are clean on the base tree.

**Reference scan (`repo-refs --check`): clean.**
778 tracked files scanned. 17 findings — 13 unreferenced files and 4 package
findings — every one covered by a row of
`tests/fixtures/repo-refs-baseline.json`, **0 uncovered**. 214 unreferenced files
sit on the never-delete list. 29 unreferenced exports (reported, not a `--check`
finding).

**Claims audit (`claims:audit`): clean on both halves.**
Phrases: 18 files, 10 banned, 5 allowed, 11 exempt, **0 violations**. Dead paths:
46 files, 6526 path references checked, 6259 resolved, 267 exempt, 156 skipped as
not repo-relative, 0 outside the repository, **0 missing**.

Every one of those figures is exactly what `hyg-audit-09` measured on its own
*staged* tree. Its writes are now merged, so its staged reading is this tick's
base reading, and the merge changed nothing in between — which is the weaker and
correct claim, and all that a comparison across a merge can establish.

With this tick's own writes in scope — this report and the `spec.md` edits, both
staged — the same two scans read: phrases unchanged at 18 / 10 / 5 / 11 / **0**;
dead paths **47** files, **6566** references, **6299** resolved, 267 exempt,
**161** skipped, still **0** missing. `repo-refs --check` still reports 17
findings, 0 uncovered and 29 exports, with the scanned-file count moving 778 →
**779** and the never-delete list 214 → **215**, because this report is itself an
unreferenced file that the `docs/hygiene/` prefix rule keeps
(`scripts/repo-refs.mjs:123`). The scan set is `git ls-files`, so both move
together on staging.

**Every figure in that paragraph was measured on the fully staged tree, not
predicted — and the two dead-path counts have a self-reference problem the
previous tick got wrong once and wrote up so the next tick would not.** This
report is itself inside the dead-path scan set and `spec.md` is not, so the whole
delta over the base tree comes from this file: every edit that adds a path-shaped
span to it moves the count, including the edits that report the count. The pair
published above is a **final measurement taken after the last prose edit**,
applied as a digit-only substitution, which cannot move the count again because a
bare number is not path-shaped.

`npm run typecheck` exits 0.

**`npm test` is RED at vitest's default worker count on this 4-CPU runner, and
green at `--maxWorkers=2`.** The default run: **102 test files, 1373 tests, 3
failed / 1370 passed**, and all three failures are the same three tests in
`tests/kb-graph.test.ts` that carry vitest's default 5 s timeout while two
neighbours in the same `describe` block carry `30_000`. That is question 122,
reproduced exactly and — this time — with all three timing out rather than two.
The same file run alone passes **10/10 in 31.5 s**. The full suite at
`--maxWorkers=2` is green: the figures are in the closing section below. This is
a load-dependent flake in a file whose failing cases include a RED TEAM
entitlement assertion, not a regression: `git diff a99f4a5..HEAD` touches no
source, no test and no dependency.

The other guards — `spec-lint` (105 items on the base tree, 106 once this tick's
re-arm is in), `landing-tier` (A), `loop-guard` (all rails pass),
`policy-guard`, `approval-path-guard` and `docs-cite-check` — all pass.

*Runner note, in the form the previous three reports established.* On this runner
the Docker daemon is **not running at session start** and `node_modules` is
**absent**, so `dockerd`, `npm install` and `npx prisma generate` all precede
`npm run typecheck`. Three things this tick adds to the record in questions 40,
44, 64, 120 and 121, none of which is a repository defect:

- **Question 120(a)'s daemon-level mirror is confirmed a second time and is the
  route that works.** Writing `{"registry-mirrors": ["https://mirror.gcr.io"]}`
  to `/etc/docker/daemon.json` before starting `dockerd` pulled
  `pgvector/pgvector:pg17`, `postgres:17-alpine` and `adobe/s3mock:latest`
  unmodified, with no retag and no edit to `docker-compose.test.yml`.
- **Question 121's compose project name held.** The stack was brought up as
  `docker compose -f docker-compose.test.yml -p servo-test up -d` on the first
  attempt and `tests/backup-restore.test.ts` passed; this tick did not have to
  find the trap the hard way, which is what recording it was for.
- **`npm install` failed once with `ECONNRESET` mid-transfer and left no
  `node_modules` behind while reporting through a shell that exited 0.** The
  retry with `--fetch-retries=8 --fetch-retry-maxtimeout=120000
  --fetch-timeout=600000` succeeded. Worth the line because the failure is
  silent in the direction that matters: a later `npx prisma generate` fetched
  `prisma@8.0.0-rc.12` from the network rather than using the pinned devDependency
  `prisma@^6.3.0`, and answered `CLI.UNKNOWN_COMMAND` for `generate`. Running
  `./node_modules/.bin/prisma generate` after a successful install is the form
  that cannot do that.

## What changed since hyg-audit-09

**Nothing but that tick's own footprint.** `git log --oneline a99f4a5..HEAD` is
exactly two commits — `1400c8b` (its work) and `8b5e7ba` (its `--no-ff` merge) —
and `git diff --stat a99f4a5..HEAD` is 2 files, +422/−0:
`docs/hygiene/audit-2026-09-03-3.md` added at 392 lines and `spec.md` at +30. No
source file, no test, no script, no schema and no dependency moved;
`package.json` and `package-lock.json` are byte-identical across the range.

`tests/fixtures/repo-refs-baseline.json` is byte-identical to the version
`hyg-audit-09` read: `git diff a99f4a5..HEAD -- tests/fixtures/repo-refs-baseline.json`
is empty. 13 file rows and 4 package rows. **This tick added, removed and edited
no baseline row** — every one of the 17 findings is already covered, and the one
file this tick creates (`docs/hygiene/audit-2026-09-03-4.md`) lands on the
never-delete list under the `docs/hygiene/` prefix rule, so it is not a finding
and needs no row.

The keep-list was checked row by row rather than trusted from the `0 uncovered`
summary line, by driving the scanner's own `parseBaseline` and `checkBaseline`
functions directly: **0 problems, 17 covered, 0 violations, 0 stale rows, 0
unjudgeable files.** All 17 rows are in exact 1:1 correspondence with the 17 live
findings. Nine rows still carry an owner whose backlog item is already `done`;
that is question 100, already recorded, and the count has not moved.

One thing worth recording because it looks like a defect and is not: the
keep-list's package rows live under a JSON key named `packages`, while
`checkBaseline` reads `baseline.dependencies`. `parseBaseline`
(`scripts/repo-refs.mjs:1592`) maps one to the other deliberately, and the
comment above it gives the reason — a JSON file carrying a literal
`"dependencies":` key makes `scripts/landing-tier.mjs` read every commit touching
it as a runtime dependency change and land Tier C. Driving the two functions
directly confirms the four package rows do cover the four package findings.

## Findings

Both scanners being clean, and the tree being two commits and 422 added lines
away from the previous audit's, is why this tick spent its capacity on surfaces
neither scanner reads. Six lenses, chosen to avoid ground the existing 123
questions already hold:

1. the CI workflow and the repository's own automation, read as falsifiable claims;
2. test discovery and test integrity — tests that never run, or run and cannot fail;
3. the configuration surface: `.env.example`, `Dockerfile`, the compose files and every `process.env` read in the tree;
4. the runtime data the app loads at boot — `agents/`, `skills/` and their loaders;
5. the adopt-first licence ledger against the actual dependency tree;
6. the design system as §0.5's source of truth.

**Every candidate was verified adversarially, then again by hand.** Each lens
returned at most four findings. Each finding was handed to a separate agent that
had seen neither the finder's reasoning nor the other findings, told to re-run
every command itself, to check every coordinate itself, to hunt actively for the
innocent explanation, to read §14 in full for prior art, and to default to
REFUTED where it could not independently confirm. **Twenty candidates, twenty
verdicts: 18 CONFIRMED, 1 REFUTED, 1 ALREADY_RECORDED.** Those twenty verdicts
are not committed to this repository — they live in the tick's transcript — so
the tally is provenance, not something a reader of this file can re-check. What a
reader can check is the seven questions below and the two kills. (The eighteen
confirmed candidates are written up as nineteen lettered-or-standalone entries
across questions 124-130: the single Docling-configuration candidate became two
sub-points, 126b and 126c, because it names two independent facts.)

The two non-confirmations did their job:

- **REFUTED: "LGPL-3.0-or-later libvips ships in the runtime tree, unlisted by
  the licence ledger."** Every raw fact reproduces —
  `@img/sharp-libvips-linux-x64` and `@img/sharp-libvips-linuxmusl-x64`, both
  `LGPL-3.0-or-later`, arrive as optional dependencies of `next → sharp` and are
  in the image. The refuter read the two gates the finding cited and neither
  says what the finding needed it to say: §0.4's rejection list governs
  *adopting* a component into this codebase, not what an npm transitive
  optional dependency of a framework drags in, and `THIRD_PARTY.md`'s own scope
  clause is narrower than "everything in `node_modules`". Killed before it
  became a question. The adjacent finding that survived — question 125 — is a
  *different* package with a *different* defect: no licence at all, and a
  non-optional production dependency.
- **ALREADY_RECORDED: "CI runs on no branch but `main`, so every Tier-A/B item
  reaches `main` before any CI run exists."** True — `.github/workflows/ci.yml`
  fires on `push: branches: [main]` and `pull_request: branches: [main]` only,
  and the file's own header says "every push and PR". It is question 79(c),
  already written down in those terms.

**Seven questions, 124 to 130, carry the eighteen confirmed findings** (nineteen
sub-points, per the split noted above). Nothing was fixed: this item deletes
nothing, moves nothing and writes no backlog item.
Each carries a keep assumption. In outline:

- **124 — SECURITY. With `AUTH_SECRET` unset, Servo signs its session cookies
  with a constant committed to this repository.** `src/lib/authjs.ts:70` is
  `secret: process.env.AUTH_SECRET ?? "servo-insecure-dev-secret-set-AUTH_SECRET"`.
  `.env.example:77` calls the variable "REQUIRED in production", `SECURITY.md:48`
  and `README.md:115` tell operators to set it — and nothing refuses to boot,
  warns, or logs when they do not. `AUTH_SECRET` appears nowhere in `spec.md`.
- **125 — two defects in the licence ledger.** `buffers@0.1.1` is in the
  *production* tree (`exceljs → unzipper → binary → buffers`) with no `license`
  field, no LICENSE file and no licence text in its README — and `Dockerfile:13`
  runs a full `npm ci`, so it ships in the image. Separately,
  `THIRD_PARTY.md:264` sources the AWS copyright to "NOTICE file in the same
  package" and the installed `@aws-sdk/client-s3` has no NOTICE file.
- **126 — the Docling lane's operator surface.**
  `scripts/record-docling-fixture.mjs` cannot run under `node`, the interpreter
  its own usage line at `:12` prescribes: `:41` dynamically imports a TypeScript
  module and plain Node exits 1 with `ERR_UNSUPPORTED_TYPESCRIPT_SYNTAX`. Its one
  test never reaches that line because it forces `CI=true` and the refusal at
  `:27` fires first. Separately, the seven environment variables that configure
  the lane appear in no `.env.example` entry and no markdown file, while the one
  key name the documents *do* publish, `SERVO_DOCLING_API_KEY`, is read only by
  that same broken script — the application reads
  `KB_EXTRACT_DOCLING_API_KEY`.
- **127 — three test-integrity defects.** `tests/docs-cite.test.ts:27` appends a
  dead citation to the *tracked* `docs/plugins.md` and restores it at `:39`,
  while `vitest.config.ts` sets nothing that would stop files running in
  parallel and `tests/claims-audit.test.ts` reads `docs/**/*.md` off disk inside
  its own assertions. `tests/docling-live-isolation.test.ts:36-38` derives the
  patterns for spec.md:1916's lane-separation proof by regex over the *text* of
  `vitest.config.ts` and never checks the list is non-empty, so a single-quoted
  rewrite makes the test pass while asserting nothing.
  `tests/kb-docling-chunker.test.ts:183-184` are two `toBeDefined()` assertions
  that hold for any implementation, in the only test of the "same unchanged code
  paths as baseline" claim.
- **128 — the bundled runtime data does not fit together.** Measured on a
  throwaway database, not argued: `syncAgentProfiles()` creates the four
  `agents/*.md` in filename order, and `pickAgentProfile()` returns the first
  enabled profile by `createdAt asc`, so `developer-agent` (`[SOFTWARE, DEVOPS]`)
  shadows `frontend-agent` (`[SOFTWARE]`) and the bundled Frontend profile can
  never be selected. The bundled skill `skills/shipping-a-code-change/SKILL.md`
  (`categories: [SOFTWARE, DEVOPS]`) instructs `github_read_file`,
  `github_edit_file`, `take_screenshot` and `github_merge_pr` — four tools the
  Developer profile does not grant and the unreachable Frontend profile does.
  `PATCH /api/agents/[id]` recomputes and writes the slug, so renaming a bundled
  profile makes the next boot re-create the shipped original beside it — the
  exact failure the sibling skills route documents itself as preventing. And
  `prisma/seed-demo.ts` contains the word "skill" zero times, so `npm run demo`
  produces a desk with no Skill rows at all.
- **129 — four colour-documentation defects.** `scripts/dev/color-audit.mjs`
  still hard-codes the retired green OKLCH palette in a table its comment calls a
  mirror of `globals.css`, which now contains zero `oklch` occurrences — so
  `docs/DESIGN.md:21-24`'s "✅ 0 fails" verifies a palette that is in no shipped
  token file, and nothing runs the script anyway. The note "white on servo blue
  — 7:1" at `servo_design_system/tokens/colors.css:75` sits in the dark `:root`
  block, where `--brand` is `--accent-500` `#4E66E4` and the real ratio is
  **4.83:1**; 7:1 is the *light* block's figure. `servo_design_system/github.md:11`
  says the brand direction is "signal green on near-black" while `readme.md:18-19`
  and the tokens say servo blue on cool graphite. And `docs/DESIGN.md`'s palette
  table maps dark `--bg` to `--ink-900` where `colors.css:47` maps it to
  `--ink-950`, and routes status badges through `@/components/legacy/Badge`, a
  directory `hyg-06` renamed away.
- **130 — two documentation-vs-reality config defects.** `README.md:88` states
  the floor as "Node.js 20+" while `puppeteer-core` declares `>=22.12.0` and
  `unpdf` declares `>=22`, both in `dependencies`, and CI pins 22 so the claimed
  floor is never tested. `docs/design/postgres.md:129` and `:131` quote
  ops-sandbox DSNs under the present-tense sentence "`.env.example` ships:" with
  the password `servo_ops`, which appears in no file in the repository —
  `.env.example:8,12` ship `servo_ops_rw`/`servo_ops_ro` and
  `scripts/postgres-init.sql:15-16` set exactly those. The same wrong password is
  at `:58`.

## Re-arm

Per the item's fifth criterion, `hyg-audit-10` is set `done` and
`hyg-audit-11` — identical acceptance, `status: todo`, `date: -` — is appended
at the END of §11. Append-only, no forward reference, no id collision;
`scripts/spec-lint.mjs` stays green with no change to its rules, reading 106
items after the re-arm.
