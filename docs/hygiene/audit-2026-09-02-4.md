# Hygiene audit — 2026-09-02 (hyg-audit-06)

Generated by `node scripts/repo-refs.mjs --evidence` and `npm run claims:audit`,
per the item's acceptance. The `-4` suffix continues the same-calendar-day
precedent [audit-2026-08-30-2.md](audit-2026-08-30-2.md) set for `hyg-audit-02`;
this is the fourth audit to run on 2026-09-02, after
[audit-2026-09-02.md](audit-2026-09-02.md),
[audit-2026-09-02-2.md](audit-2026-09-02-2.md) and
[audit-2026-09-02-3.md](audit-2026-09-02-3.md).

No `-evidence.md` companion is committed. `hyg-audit-05` stopped committing one
and recorded why as question 75; this tick follows that precedent and generated
its evidence to a path outside the repository.

This tick ran in the ORDINARY pick order, not by a cadence jump. It deleted
nothing, moved nothing, appended no backlog content beyond its own re-arm, and
added no baseline row — nothing qualified.

## The pick chain, re-derived and then attacked

The chain was re-derived from the tree and from GitHub rather than inherited,
and then put to four independent adversarial agents, each told to DISPROVE it
from a different angle: the literalist reading of §0.2 step 4, the Tier-C
loophole, the three `blocked` items, and the audit item itself. **All four
attacks failed. None broke the claim.**

- `hyg-05` holds the review slot. PR #27 (`feat/hyg-05`, head `30e6865`) has been
  open and unmerged since 2026-09-01T09:50:32Z and is the only open PR in the
  repository, so §0.3's state table makes it `review` however `main`'s block
  reads — the question-55 gap, seventh consecutive tick. §0.6's "Skip, never
  merge" forbids re-implementing work already complete on the branch.
- `hyg-06` is dependency-blocked on it, and no `proceed-on-branch: hyg-05` line
  exists under §14.
- `hyg-07` and `hyg-09` are Tier C; each would be §0.6's forbidden second open
  Tier-C PR.
- `dcl-07` is `blocked` (no Docker on this runner, question 71) and `dcl-08`
  depends on it. `ext-05` is `blocked` (question 59). `fed-06` reads `blocked`
  and has no `todo` dependent.
- That leaves `hyg-audit-06`.

**The cadence case, as the item's fourth criterion requires the commit message
to state: NOT DUE ON EITHER HALF.** §15 holds 135 rows on `origin/main` and the
`hyg-audit-05` row is row 135 — the last line of the table — so **zero** rows
have accrued against a threshold of twenty. The second half is false too while
#27 is open. No jump was available in any case: the §0.2 step 4 sentence that
would let a due audit jump is owner-applied and still absent (question 34c).

## Verdicts

**Reference scan (`repo-refs --check`): clean.**
767 tracked files scanned **on the base tree** — 768 with this report in scope.
20 findings — 15 unreferenced files and 5 package findings — every one covered
by the 15 `files` rows and 5 `packages` rows of
`tests/fixtures/repo-refs-baseline.json`, 0 uncovered, on both trees.
215 unreferenced files sit on the never-delete list (216 with this report).
37 unreferenced exports on both trees (not a `--check` finding).

**Claims audit (`claims:audit`): clean on both halves.**
Phrases: 17 files, 10 banned, 5 allowed, 11 exempt, 0 violations. Dead paths on
the base tree: 40 files, 5220 references checked, 5188 resolved, 32 exempt, 123
skipped as not repo-relative, 0 outside the repository, **0 missing**. With this
report and this tick's spec.md edit in scope the same scan reads 41 / 5226 /
5194 / 32 / 125, still **0 missing**; the changelog row publishes both.

## What changed since hyg-audit-05

**Nothing a scanner can see.** `git diff 0c83edf origin/main` is empty: the tree
`hyg-audit-05` produced is byte-identical to the tree this audit scanned. The
only delta against the tree `hyg-audit-05` *scanned* is its own two files —
`docs/hygiene/audit-2026-09-02-3.md` (+209) and `spec.md` (+27). File count
766 → 767, never-delete list 214 → 215, findings held at 20, unreferenced
exports held at 37. The single new file is on the never-delete list, so **no
baseline row was added, removed or edited.** Canon movements: none; the two
transitional `paths-exempt` groups still wait on `dcl-07` and `hyg-09`.

That is the fifth consecutive tick with no source change and the fourth with
identical scanner findings — questions 74 and 76 already record it, and
question 79 below records the mechanism that keeps it that way.

## Findings

Both scanners being clean is why this tick spent its capacity elsewhere. Six
independent lenses were run over surfaces neither scanner reads — the keep-list's
prose, the claims canon's own self-description, spec.md's bookkeeping against the
tree, the guard scripts against the rails they implement, the audits' own past
claims, and the repository's live state on GitHub — producing 45 candidate
findings. Forty were then handed to independent adversarial verifiers who had not
seen the finder's reasoning and were told to refute them and to reject anything
already recorded under §14. **Thirty-five survived**; five were refuted, and five
candidates beyond the fortieth were not verified and are excluded rather than
reported unchecked.

They are recorded as **questions 79 to 93** under §14, grouped by mechanism, each
with a keep assumption. Nothing was fixed: this item deletes nothing, moves
nothing and writes no backlog item. The headline ones:

- **79** — PR #27 cannot be landed as it stands, on two independent counts, and
  every tick the loop takes while waiting makes the first one worse.
- **80, 81** — two landing rails do not implement §0.6 as written. The migration
  guard's divergence has already auto-merged a statement §0.6 puts verbatim in
  its Tier-C list.
- **82, 83** — `loop-guard`'s rails 1 and 3 are narrower than §0.8's wording;
  rail 1 is inert against every Postgres database this repository documents.
- **84 to 88** — §1.1, §1.3 and §2 of spec.md describe a repository that no
  longer exists, and a dead item id is cited by a safety rail.
- **89, 90** — the keep-list's and the canon's own prose have drifted from what
  they describe.
- **91 to 93** — CI and repository health, and two smaller protocol seams.

## Method note

The verification pass, not either scanner, found every finding above. Neither
scanner can read a citation, a line coordinate, a prose reason, or anything on
GitHub — which is why this defect class has no automated check, exactly as
question 78 observed. Fifty agents, 1,349 tool calls; the pick chain and every
reported finding were independently reproduced before being written down.
