# Hygiene audit — 2026-09-03 (hyg-audit-09)

Generated by `node scripts/repo-refs.mjs --evidence` and `npm run claims:audit`,
per the item's acceptance. Third audit on this calendar day, so the `-3` suffix
continues the convention [audit-2026-08-30-2.md](audit-2026-08-30-2.md) set:
`hyg-audit-07`'s report is [audit-2026-09-03.md](audit-2026-09-03.md) and
`hyg-audit-08`'s is [audit-2026-09-03-2.md](audit-2026-09-03-2.md); neither is
overwritten.

No `-evidence.md` companion is committed. `hyg-audit-05` stopped committing one
and recorded why as question 75; this tick follows that precedent and generated
its evidence to a path outside the repository.

This tick ran in the ORDINARY pick order, not by a cadence jump. It deleted
nothing, moved nothing, appended no backlog content beyond its own re-arm, and
added no baseline row — nothing qualified.

**One thing about this tick was not routine, and it is the headline.** `main`'s
CI was **red** when the tick began, on the very commit the previous audit
merged. Under the rail the owner accepted in question 49, that forbids merging a
Tier-A branch. The tick did not merge onto red and did not pretend the red was
not there; it is written up below and as question 119.

## The pick chain

Re-derived from `origin/main` at `a99f4a5` and from the GitHub API, not
inherited from the previous report. Every coordinate and count in this section
was produced by one agent and then re-run by a second agent told to refute it.

**Every `spec.md` line number in this report is measured against `a99f4a5`, the
tree the tick read, and every one of them shifts when this tick's own diff
lands** — by +18 above §15 (the re-arm block) and by +29 below it (the re-arm
plus questions 119-123). This is question 104's convention, adopted here for the
same reason, and it is not a formality: `hyg-audit-08` published nine coordinates
that were pre-insert and had to correct them in a follow-up commit, and this
tick's own independent verification caught the same class of error again before
it landed. Read the coordinates against `a99f4a5`, or by the quoted text.

- **104 backlog items carry a status; 103 read `done`**, and the only `todo` item
  in the whole file is `hyg-audit-09` (block header at spec.md:2364, `status:` at
  :2365). On the landed tree those figures become 105 and 104, and the only
  `todo` becomes `hyg-audit-10`, because this tick flips `hyg-audit-09` to `done`
  and appends its successor. The two other `status: todo` lines, at spec.md:48 and spec.md:394,
  belong to the format TEMPLATE blocks headed at :47 and :393, which carry the
  literal id `<id>` and are not items. `node scripts/spec-lint.mjs` prints
  `OK (104 items)`, which is the same 103 + 1.
- **Its dependencies are satisfied**: `hyg-01` (`done` 2026-08-27, header at
  spec.md:1600) and `hyg-04` (`done` 2026-08-29, header at spec.md:1659). Its
  `depends-on` line is spec.md:2369.
- **Nothing is `doing`, `blocked` or `review`.** `grep -cE "^status: (review|doing|blocked)" spec.md`
  returns `0`, and the GitHub API's open-pull-request list for `ricauts/servo`
  is empty, so the review slot is free and nothing is waiting in it.
- So §0.2 step 4 selects `hyg-audit-09`, and there is no second candidate. The
  anti-stall provisions of §0.6 are all inert this tick.

**The cadence case, which the item's fourth criterion requires the commit message
to state: NOT DUE, and the tick ran in the ORDINARY pick order anyway.** §15
holds 150 data rows (lines beginning `| 2026-`, running from spec.md:2979 to
spec.md:3136). The most recent `hyg-audit-*` row is `hyg-audit-08`'s at
spec.md:3136, and it is the **last row in the table** — `awk 'NR>3136 && /^\| 2026-/' spec.md`
returns nothing — so **0** rows have accrued against a threshold of twenty. The
second half of the cadence test, no item in `review`, is satisfied. The row
count is not, so the item is not DUE; it did not need to be, because with every
other item `done` the ordinary pick order reaches it anyway. No jump was taken
and none was available: the §0.2 step 4 sentence that would permit one is
owner-applied and still absent (question 34c).

Counting by the row's item-id column is what the cadence clause means, and the
`hyg-audit-*` rows sit at spec.md:3066, 3076, 3116, 3117, 3118, 3119, 3135 and
3136. Two properties of that table are worth recording because they could make a
future count wrong even though they do not make this one wrong: the rows are
**not in date order** (spec.md:3039 is 2026-08-29 while :3041 is 2026-08-28;
:3098–3099 are 2026-09-01 while :3102–3105 are 2026-08-31), and the table is
**broken into fragments** by blank lines at :3042, :3044, :3056, :3100, :3101,
:3127 and :3128 plus the stranded question at :3129. This tick counted by file
order. Here the two readings agree — spec.md:3136 is both the last row in the
file and tied for the latest date — so NOT DUE is robust either way.

**Landing rail check, per the owner answer of 2026-08-28 recorded under question
49 — and this is the part that did not go the usual way.** The rail: the loop
refuses to merge a Tier-A/B branch while `main`'s own latest CI run is failing.
At the moment this tick started, `main`'s latest run was **run 243
(`33720296579`, head `a99f4a5`, attempt 1), concluded `failure`** — the CI run of
the merge commit `hyg-audit-08` itself created. The previous report's rail
paragraph cited run 242 on `976ddbb` as green, which was true when it was
written; the state flipped underneath it when that tick merged.

The failure is `tests/federation-e2e.test.ts` → "the two-silo world" dying on
PostgreSQL `57P01` (*terminating connection due to administrator command*) inside
`seedWarehouse` at `tests/setup/silos.ts:124` — a connection fault during setup,
not a defect the previous tick's docs-and-spec diff could have caused, and not
something any commit on this branch could fix. CI otherwise corroborated the
previous report's suite totals exactly: 102 files and 1373 tests, differing only
in outcome (1 failed / 1372 passed).

**What the tick did about it: re-ran the failed job, once, and waited.** Attempt
2 of run 243 completed `success` at 2026-09-03T09:07:24Z, every step green —
Typecheck, Unit tests, `lint:hex`, `claims:audit`, `hygiene:check` and
Production build. `main`'s latest CI run therefore now concludes `success` and
the question-49 rail is satisfied, on the evidence rather than on an assumption
that a flake would clear. A single re-run passing on the identical commit is
what makes "infrastructure fault, not a defect" a measurement rather than a
hope. This is written up as question 119, together with the observation that runs 219,
226 and 237 concluded `failure` on `main` and 243 did so on its first attempt.
Run 237 is the half worth stating carefully, because it cuts the other way: it
was re-run three times, failed all four attempts, and was **never** made green by
a re-run — `main` recovered only via the separate fix commit that produced run
238. A re-run is a measurement, not a remedy.

**Adopt-first gate (§0.4):** nothing to adopt. This item writes one report, spec
prose and (conditionally) baseline rows; it adds no component, so the gate has
nothing to rule on. No dependency was added, removed or bumped, and
`package.json` is untouched.

**Design-system read (§0.5):** not triggered. The diff is `docs/` and `spec.md`
only and touches no UI surface.

## Verdicts

Both scanners are clean on the base tree.

**Reference scan (`repo-refs --check`): clean.**
777 tracked files scanned. 17 findings — 13 unreferenced files and 4 package
findings — every one covered by a row of
`tests/fixtures/repo-refs-baseline.json`, **0 uncovered**. 213 unreferenced files
sit on the never-delete list. 29 unreferenced exports (reported, not a `--check`
finding).

**Claims audit (`claims:audit`): clean on both halves.**
Phrases: 18 files, 10 banned, 5 allowed, 11 exempt, **0 violations**. Dead paths:
45 files, 6504 path references checked, 6237 resolved, 267 exempt, 149 skipped as
not repo-relative, 0 outside the repository, **0 missing**.

Both readings land exactly where `hyg-audit-08` left them. That tick published
776/212 on its base tree and 777/213 with its own writes staged; its writes are
now merged, so its staged reading is this tick's base reading, and 777/213 is
what this tick measures. The claims dead-path counts match its staged reading too
(45 / 6504 / 6237 / 267 / 149 / 0). Those figures were **measured after staging
by that tick, not predicted** — its own changelog row says so in terms — so what
this tick confirms is the weaker and correct claim: the merge changed nothing
between that tick's staging and this tick's base.

With this tick's own writes in scope — this report and the `spec.md` edits, both
staged — the same two scans read: phrases unchanged at 18 / 10 / 5 / 11 / **0**;
dead paths **46** files, **6526** references, **6259** resolved, 267 exempt,
**156** skipped, still **0** missing. `repo-refs --check` still reports 17
findings, 0 uncovered and 29 exports, with the scanned-file count moving 777 →
**778** and the never-delete list 213 → **214**, because this report is itself an
unreferenced file that the `docs/hygiene/` prefix rule keeps. The scan set is
`git ls-files`, so both move together on staging. Every figure in this paragraph
was **measured on the fully staged tree, not predicted** — and the two dead-path
counts have a self-reference problem worth spelling out, because this tick got
them wrong once and the mechanism will catch the next tick too. **This report is
itself inside the dead-path scan set, and `spec.md` is not**, so the entire delta
over the base tree comes from this file: every edit that adds a path-shaped
reference to it moves the count by one, including the edits that report the
count. The first published pair, 6524/6257, was measured before a later edit; the
independent verifier of this tick re-ran `claims:audit`, got 6525/6258,
reproduced it four times and on a pristine `git archive` copy, and isolated the
cause exactly there. The pair published above is a **final measurement taken
after the last prose edit to this file**, applied as a digit-only substitution,
which cannot move the count again because a bare number is not path-shaped. The
verifier's 6525/6258 and the figure above differ for the same reason the original
6524/6257 did: prose was added in between.

An independent auditor re-derived every quantitative claim in
`audit-2026-09-03-2.md` — on the base tree via a `git worktree` at `976ddbb` and
on the current tree — and **could not falsify a single scanner,
count, coordinate or git figure in it**. It also spot-checked all nine of the
spec.md coordinates that report's own follow-up commit `b924306` renumbered for
questions 107–118, plus its three citation fixes; none is wrong at `a99f4a5`.
`hyg-audit-08`'s numbers are sound and its thirteen self-corrections all landed
correctly. That is worth saying plainly, because the value of this audit is
mostly in the checks that come back negative.

Two honest qualifications on that sentence. **It is scoped to quantities, and one
non-scanner figure in that report is wrong**: it described §14 as holding "106
existing questions", where 106 is the highest question *number* and the section
held 123 entries. That is question 123's subject, and it means a verifier told to
search 106 questions under-searched by 17. And **the nine renumbered coordinates
are correct only at `a99f4a5`**: this tick's own re-arm shifts every one of them
again, which is the recurring breakage question 123 exists to report rather than
a defect in that report.

`npm run typecheck` exits 0. `npm test` is green: **102 test files, 1373 tests,
0 failures**, run against real `pgvector/pgvector:pg17`, `postgres:17-alpine`
and `adobe/s3mock:latest` containers on this runner — but green only at
`--maxWorkers=2`. At vitest's default worker count on this 4-CPU runner, three
tests in `tests/kb-graph.test.ts` time out. They are not defects and they pass
10/10 when that file runs alone; the cause is that those three carry vitest's
default 5 s timeout while two neighbours in the same `describe` block carry
`30_000`. Measured, not assumed — and the count is load-dependent, stated
precisely rather than rounded up: across three full runs at the default worker
count this tick saw all three time out on one run and two of the three on
another, and the independent verifier of this tick, running the suite itself on
this machine, also saw two. 0 failures at `--maxWorkers=2`; 10/10 in isolation.
Question 122.

The other guards — `spec-lint` (104 items on the base tree, 105 once this tick's
re-arm is in), `landing-tier` (A), `loop-guard` (all rails pass),
`policy-guard`, `approval-path-guard` and `docs-cite-check` — all pass.

*Runner note, in the form the previous two reports established, because each
time it has saved the next tick most of an hour.* On this runner the Docker
daemon is **not running at session start** (`docker info` fails with
`dial unix /var/run/docker.sock: no such file or directory`) and `node_modules`
is **absent**, so `dockerd`, `npm install` and `npx prisma generate` all precede
`npm run typecheck`. Question 40's Docker Hub image-pull denial still holds
exactly as described — `production.cloudfront.docker.com` answers 403 to CONNECT,
`curl` exit 56 — and question 44's `mirror.gcr.io` route still works. Three
things this tick did differently or found newly, all in question 120: the mirror
was configured **at the daemon** (`/etc/docker/daemon.json` holding
`{"registry-mirrors": ["https://mirror.gcr.io"]}`) rather than by retagging each
image, which needs no retag and let `docker compose` run the compose file
completely unmodified; `node:22-alpine` pulls through it, which closes the half
of question 44 that was left explicitly open; and the stack **must** be brought
up as project `servo-test` (`docker compose -f docker-compose.test.yml -p servo-test up -d`),
which question 64 states and which this tick proved by getting it wrong first —
see question 121.

## What changed since hyg-audit-08

**Nothing but that tick's own footprint, which is the point of saying so.**
`git log --oneline 976ddbb..HEAD` is exactly three commits — `8a21348` (its
work), `b924306` (its thirteen self-corrections) and `a99f4a5` (its `--no-ff`
merge) — and `git diff --stat 976ddbb..HEAD` is 2 files, +324/−0:
`docs/hygiene/audit-2026-09-03-2.md` added at 281 lines and `spec.md` at +43. No
source file, no test, no script, no schema and no dependency moved;
`package.json` and `package-lock.json` are byte-identical across the range.

`tests/fixtures/repo-refs-baseline.json` is byte-identical to the version
`hyg-audit-08` read: 13 file rows and 4 package rows. **This tick added, removed
and edited no baseline row** — every one of the 17 findings is already covered,
and the one file this tick creates (`docs/hygiene/audit-2026-09-03-3.md`) lands
on the never-delete list under the `docs/hygiene/` prefix rule
(`scripts/repo-refs.mjs:123`), so it is not a finding and needs no row.

The keep-list was additionally checked row by row rather than trusted from the
`0 uncovered` summary line, by driving the scanner's own `checkBaseline` and
`parseBaseline` functions directly: **0 stale rows**. All 17 rows are in exact
1:1 correspondence with the 17 live findings — no row covering nothing, no
finding lacking a row. Nine rows carry an owner whose backlog item is already
`done`; that is question 100, already recorded, and it is an observation rather
than a defect, because the keep-list's own `$schema-note` forbids removing a row
outside the commit that removes the thing or references it.

## Findings

Both scanners being clean, and the tree being three commits and 324 added lines
away from the previous audit's, is why this tick spent its capacity on surfaces
neither scanner reads: the pick chain and the spec's own bookkeeping; the
keep-list driven through its own parser; the previous report re-derived
end-to-end on two trees; and the runner and CI read as claims that could be
false.

**Every candidate was verified adversarially** — a second agent per *claim*,
given only that claim and its cited evidence, told to re-run the commands itself
and to default to REFUTED where it could not independently confirm a number. The
unit counted here is the claim, not the finding: the four auditors produced
claims across four lenses, 48 of those claims were put to a refuter, and the
verdicts were **38 STANDS, 8 PARTLY_WRONG, 2 REFUTED**. Those 48 verdicts are not
committed to this repository — they live in the tick's transcript — so the tally
is stated as provenance, not as something a reader of this file can re-check.
What a reader can check is the five questions it produced and the corrections
listed below. The ten non-STANDS verdicts
did their job, and three of them changed what this report says:

- A candidate finding that `adobe/s3mock` was entirely new to the spec was
  **REFUTED**: spec.md:3136 records a green 1373-test run against real
  `adobe/s3mock` containers and spec.md:3092 records a crawl against a real
  s3mock. What is actually new is far narrower — only that no manifest digest is
  recorded for it anywhere. Question 120 says the narrow thing.
- A candidate finding that the media allowlist in `docs/MEDIA-GUIDE.md` is inert
  was **REFUTED**: there are two parsers, and the live one
  (`mediaImportAllowlist`, `scripts/repo-refs.mjs:944`) hardcodes the fence name
  the guide actually uses and returns `['sharp','ffmpeg-static']`. Only the newer
  `parseMediaAllowlist` is dead against the real guide. Question 101 already owns
  that mismatch **and already draws the two-parser distinction explicitly**,
  naming both readers and the `['sharp','ffmpeg-static']` return. So this is not
  a refinement of question 101; it is this tick's own candidate having restated
  question 101's finding too broadly, caught before it became a question.
- Two candidates — the stranded questions 94–97 and the "98 items" scope note —
  were **already recorded**, as questions 104 and 99 respectively. They are not
  repeated as new questions.

**Five findings survived as genuinely new, and they are questions 119 to 123**
under §14, each with a keep assumption. Nothing was fixed: this item deletes
nothing, moves nothing and writes no backlog item. In outline:

- **119 — `main`'s CI was red when this tick began, and the question-49 rail bit
  for the first time.** Run 243 on `a99f4a5` failed on a `57P01` inside
  `tests/setup/silos.ts:124`; the tick re-ran it rather than merging onto red,
  and attempt 2 passed. The rail exists only as prose: nothing in
  `scripts/landing-tier.mjs` consults `main`'s CI, so it holds exactly as long as
  each tick remembers to look. Runs 219, 226 and 237 concluded `failure` on
  `main` and 243 did so on its first attempt; 237 failed all four of its attempts
  and was never made green by a re-run at all.
- **120 — four corrections and extensions to the runner record in questions 44
  and 64.** The daemon-level `registry-mirrors` mechanism appears nowhere in
  spec.md; `node:22-alpine` pulls through the mirror, closing the half of
  question 44 left explicitly open (question 64's claim to have closed it tested
  only `postgres:17-alpine`); `adobe/s3mock:latest` has no digest on record
  anywhere, and this tick's is `sha256:7a37f0d7…`; and "vector 0.8.6" as stated
  in questions 44 and 64 is a statement about `pg_available_extensions` — the
  extension is **not installed** in the `postgres` database on 5433, only in
  `servo_test_template`.
- **121 — the compose project name is a trap that costs a tick to find, and the
  repository does not record it.** `docker compose -f docker-compose.test.yml up -d`
  from the repo root yields project `servo`, but
  `tests/backup-restore.test.ts:45` pins `-p servo-test` for its container
  fallback — the path taken whenever the host `psql`/`pg_dump` is older than the
  server, which on this runner it is (16.13 against 17.11). Measured: the test
  fails, then passes once the stack is recreated with `-p servo-test`.
- **122 — three tests in `tests/kb-graph.test.ts` do multi-document database work
  on vitest's default 5 s timeout while two neighbours in the same `describe`
  block carry `30_000`.** They time out on a 4-CPU runner at the default worker
  count and pass at `--maxWorkers=2` and in isolation. A latent flake, in a file
  whose failing cases include a RED TEAM entitlement assertion.
- **123 — §14's question numbers are not unique.** 135 numbered entries across
  113 distinct numbers; 15 numbers are duplicated (20–26 three times each, 27–30
  twice each, and single accidental collisions at 41, 50, 51 and 72). Every
  citation-by-number in every report is therefore ambiguous, and the reports have
  begun working around it in prose ("the second question numbered 72", question
  111).

## Independent acceptance verification

Before this tick committed, its acceptance criteria and its staged diff were put
to a second set of agents that had **not** written the work and had **not** seen
the implementer's reasoning. Each got one criterion and was told to try to prove
it NOT met, to re-run every command itself, and to default to NOT_MET where it
could not independently confirm something. Three further agents hunted factual
defects across the report, the five questions and the changelog row.

**Verdicts: five criteria MET, two PARTIALLY_MET.** Both partials were real and
both are fixed above rather than argued away:

- *Criterion 1* was PARTIALLY_MET because two published figures were wrong. The
  verifier re-ran `claims:audit` on the staged tree, got 6525 references / 6258
  resolved against the published 6524 / 6257, reproduced it four times and on a
  pristine `git archive` copy, and isolated the cause to this report file having
  gained one path-shaped reference in an edit made after the measurement. The
  structural half of that criterion — the report derived from the two named
  scanners, nothing written but the report and `spec.md`, no baseline row
  touched, no evidence file inside the repository — it confirmed in full.
- *Criterion 4* was PARTIALLY_MET only because the criterion requires the **commit
  message** to state the cadence case and no commit existed yet at verification
  time. Every number in the cadence arithmetic it did check reproduced, and it
  attacked the "most recent `hyg-audit-*` row" reading three ways (file order,
  date-tie-broken, counting the row itself) — 0, 1 and 1 accrued, all far below
  twenty.

**The defect hunt found errors in this tick's own output, and correcting them is
part of the tick.** The substantive ones:

- **"Run 237 needed four attempts to pass" was false.** Run 237 concluded
  `failure` on all four attempts and was never made green by a re-run at all;
  `main` recovered via a separate fix commit. This mattered because the sentence
  was offered as support for the claim that a re-run is a measurement — and the
  true version supports it better, which is why it now says so.
- **Every `spec.md` coordinate in the report and in questions 120 and 123 was
  pre-insert**, and would have been wrong in the file they ship in — the exact
  defect class `b924306` had to correct for `hyg-audit-08`. They now carry
  question 104's explicit "measured against `a99f4a5`" caveat.
- **Question 123's entry counts** (135 across 113) are the base-tree reading;
  the landed reading is 140 across 118, and the question now says both.
- **Question 120(d) attributed "vector 0.8.6" to questions 44 and 64**; only
  question 44 uses that string.
- **Question 121 said the host `psql`/`pg_dump` mismatch makes the fallback
  always taken.** `psql` 16.13 connects; it is `pg_dump` that refuses. The
  thrown message lives at `tests/backup-restore.test.ts:49-53`, not `:83`, which
  is the call site. And a third site records the `servo-test` requirement — the
  `xds-01` §15 changelog row — not only question 64 and the audit reports.
- **Question 122 claimed three timeouts on two independent runs.** The count is
  load-dependent; the accurate statement is now published.
- **"Could not falsify a single one"** overstated: it is true of every scanner,
  count, coordinate and git figure in `audit-2026-09-03-2.md`, but that report's
  "106 existing questions" is falsified by this tick's own question 123, and the
  report now says so.
- **The `hyg-audit-08` figures were described as "predicted".** That tick's own
  changelog row says they were measured after staging; the weaker, correct claim
  is now made.
- **Question 101 was credited with less than it holds** — it already draws the
  two-parser distinction this tick's candidate thought it was refining.

## Re-arm

Per the item's fifth criterion, `hyg-audit-09` is set `done` and
`hyg-audit-10` — identical acceptance, `status: todo`, `date: -` — is appended at
the END of §11. Append-only, no forward reference, no id collision;
`scripts/spec-lint.mjs` stays green with no change to its rules, reading 105
items after the re-arm.
