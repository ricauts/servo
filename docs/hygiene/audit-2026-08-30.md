# Hygiene audit — 2026-08-30 (hyg-audit-01)

Generated by `node scripts/repo-refs.mjs --evidence docs/hygiene/audit-2026-08-30-evidence.md`
and `npm run claims:audit`, per the item's acceptance. This tick deleted
nothing, moved nothing, appended no backlog items, and added no baseline
rows — nothing qualified.

## Verdicts

**Reference scan (`repo-refs --check`): clean.**
22 findings, all covered by `tests/fixtures/repo-refs-baseline.json`,
0 uncovered. No new unreferenced file appeared since the hyg-04 baseline was
last touched (the dcl-01 correction row, which added `src/instrumentation.ts`
after CI caught it). Full evidence: [audit-2026-08-30-evidence.md](audit-2026-08-30-evidence.md).

**Claims audit (`claims:audit`): clean on both halves.**
Phrases: 12 files scanned, 10 banned phrases in force, 5 allowed, 11 exempt
occurrences, 0 violations. Dead paths: 27 files, 1541 path references
checked, 1500 resolved, 41 exempt, 95 skipped as not repo-relative, 0
outside the repository, 0 missing. The scan grew by two files this tick
(docs/README.md via hyg-08, this evidence file) — every reference in both
resolves.

**Canon movements this tick (forced, not elective):** the `docs/hygiene`
forward-reference target left POSITIONING's paths-exempt entry — this
audit's evidence file is the directory's first file, so the design
document's directory reference resolves now. The same retirement every
delivered target takes.

## Open questions for the owner

None. The audit found nothing whose removal the loop could propose, so
there is no question to carry a keep assumption. The previously recorded
owner items stand unchanged: PR #17 (fed-04) holds the Tier-C review slot
with fed-05/fed-06 queued behind it (questions 50/51), and PR #18 (cnp-02)
was opened by a concurrent session while #17 held the slot — a one-slot
violation recorded in the 2026-08-30 hyg-08 changelog row for disposition.

## Cadence

84 changelog rows exist and none is a hyg-audit row, so the audit is DUE
by the twenty-row rule (measured from the first row, there being no
earlier audit). It ran through the ORDINARY pick order — no owner-applied
jump was in effect and no item was in review. The commit message states
this case.
