# Hygiene audit — 2026-09-02 (hyg-audit-03)

Generated by `node scripts/repo-refs.mjs --evidence docs/hygiene/audit-2026-09-02-evidence.md`
and `npm run claims:audit`, per the item's acceptance. This tick ran in the
ORDINARY pick order, not by a cadence jump: `hyg-05` holds the review slot
(PR #27, open since 2026-09-01T09:50Z), `hyg-06` is dependency-blocked on it,
`hyg-07` and `hyg-09` are Tier C and would each be the second open Tier-C PR,
`dcl-07` is `blocked` and `dcl-08` depends on it — so hyg-audit-03 is simply
the next item the §0.2 step-4 rule reaches. This tick deleted nothing, moved
nothing, appended no backlog content beyond its own re-arm, and added no
baseline rows — nothing qualified.

## Verdicts

**Reference scan (`repo-refs --check`): clean.**
764 tracked files scanned (699 at the last audit; the count includes this
report and its evidence file, which were staged before the final run so the
numbers describe the tree as it is committed). 20 findings, all covered by
`tests/fixtures/repo-refs-baseline.json`, 0 uncovered — and, in the other
direction, 0 baseline rows without a matching finding, so the keep-list is
exactly the size of the problem it describes. 212 unreferenced files sit on
the never-delete list, this report among them. Full evidence:
[audit-2026-09-02-evidence.md](audit-2026-09-02-evidence.md).

**Claims audit (`claims:audit`): clean on both halves.**
Phrases: 17 files, 10 banned, 5 allowed, 11 exempt, 0 violations. Dead paths:
37 files, 3956 references checked, 3924 resolved, 32 exempt, 111 skipped as
not repo-relative, 0 outside the repository, **0 missing**. The scan has grown
by ~1,280 references since the last audit and every one of them resolves.

## What changed since hyg-audit-02 (2026-08-30)

**Findings: 21 → 20.** The one that left was `docs/design/external-sources.md`,
which `xds-01` made a live reference from `src/lib/kb/sources.ts`; `xds-01`
removed its baseline row in the same commit, which is the rule `hyg-04` wrote.
`servo_design_system/readme.md` also stopped reading as unreferenced.

**Eleven files became unreferenced and none needs a row**, because every one
of them is on the never-delete list: the two `hyg-audit-02` reports and this
tick's own two under `docs/hygiene/`, migrations `0010`, `0011` and `0013`,
and the four files of `tests/fixtures/plugins/fixture-demo/`.

**Unreferenced exports: 37 → 37, with three in and three out.** In:
`src/lib/kb/sources.ts:41 SourceStatus`, `src/lib/kb/sources/s3.ts:224 etagOf`,
`src/lib/types.ts:107 OriginKind`. Out: `DEFAULT_RULESET` and
`EXTRACTOR_VERSION` from `src/lib/kb/facts/index.ts`, both picked up by
`ext-04`'s ingestion wiring, and `FactKind` from `src/lib/kb/facts/types.ts`,
picked up by `ext-06`'s retrieval filters and `ext-07`'s `search_knowledge`
filters. An unreferenced export is not a `--check` finding and needs no
baseline row; each of the three new ones lives in a module that is itself
alive, and two are string-union types that document a column.

**Canon movements: none forced by this tick.** The transitional
`paths-exempt` groups are down from three to two (`dcl-07`, `hyg-09`) —
`dcl-06` retired its own group when it landed the overlay and `docs/KB-DOCLING.md`,
which is the convention working as designed.

## Open questions for the owner

Two raised, both recorded rather than acted on — this tick removes nothing
and edits no baseline row. See §14 of `spec.md`:

- **Question 72 — four baseline rows name an owner item that is already
  `done`, so nothing will ever end them.** `src/lib/catalog/types.ts` (owner
  `cat-05`), `src/instrumentation.ts` (`dcl-01`), `docs/design/extraction.md`
  (`hyg-08`) and the `@types/validator` package row (`ext-02`) all name items
  that merged without removing the row, and by the nature of each file none
  of them can ever become referenced. The baseline's own convention says a
  permanent keep is owned by a `question-<n>`, not by an item.
  *Keep assumption: all four rows stay exactly as written.*

- **Question 73 — one `paths-exempt` group is scheduled for removal by an
  item that does not deliver both of its targets.** The group carrying the
  archived media directory and the dockerignore test (both named exactly in
  question 73; they are written without code spans here because neither
  exists yet and this file is inside the dead-path scan) reads `until: hyg-09`,
  but the dockerignore test is `hyg-07`'s deliverable. If `hyg-09` lands first
  and the group is retired as its `until:` says, `docs/design/hygiene.md`'s
  reference to that test goes dangling and `npm run claims:audit` — a CI step —
  goes red on `main`. *Keep assumption: the group stays as written.*

## Cadence

Changelog rows since the `hyg-audit-02` row: **37** (twenty or more) — row
ordinals in the section 15 table on `origin/main`, excluding this tick's own
row. That is exactly one more than the 36 the previous tick's row reported,
which is the cross-check that makes the figure trustworthy: an earlier draft
of this section said 29, and independent verification caught it before this
file was committed. The
twenty-row half of the cadence is therefore satisfied, but the other half —
*and no item is in review* — is not: `hyg-05`'s PR #27 has been open since
2026-09-01. The audit is consequently **not DUE**, and it ran because it was
the next item in the ORDINARY pick order. That is the acceptance criterion's
own slower-cadence case, and the commit message states it the same way.

## What this tick did not do

Deleted nothing. Moved nothing. Removed no baseline row and edited none.
Appended no backlog item except the `hyg-audit-04` re-arm the acceptance
requires. Proposed no deletion in prose — the two findings above are
questions with keep assumptions, which is the only shape this item may use.
