# Hygiene audit — 2026-09-02 (hyg-audit-05)

Generated by `node scripts/repo-refs.mjs --evidence` and `npm run claims:audit`,
per the item's acceptance. The `-3` suffix continues the same-calendar-day
precedent [audit-2026-08-30-2.md](audit-2026-08-30-2.md) set for
`hyg-audit-02`; this is the third audit to run on 2026-09-02, after
[audit-2026-09-02.md](audit-2026-09-02.md) and
[audit-2026-09-02-2.md](audit-2026-09-02-2.md).

This tick ran in the ORDINARY pick order, not by a cadence jump. The pick chain
was re-derived from the tree and from GitHub rather than inherited: `hyg-05`
holds the review slot (PR #27, open and unmerged since 2026-09-01T09:50:32Z, so
§0.3's state table makes it `review` however `main`'s block reads), `hyg-06` is
dependency-blocked on it, `hyg-07` and `hyg-09` are Tier C and each would be
§0.6's forbidden second open Tier-C PR, `dcl-07` is `blocked` and `dcl-08`
depends on it. This tick deleted nothing, moved nothing, appended no backlog
content beyond its own re-arm, and added no baseline row — nothing qualified.

## Verdicts

**Reference scan (`repo-refs --check`): clean.**
766 tracked files scanned on the base tree. 20 findings — 15 unreferenced files
and 5 package findings — every one of them covered by the 15 `files` rows and 5
`packages` rows of `tests/fixtures/repo-refs-baseline.json`, 0 uncovered, and
0 baseline rows without a matching finding in the other direction. 214
unreferenced files sit on the never-delete list.

**Claims audit (`claims:audit`): clean on both halves.**
Phrases: 17 files, 10 banned, 5 allowed, 11 exempt, 0 violations. Dead paths on
the base tree: 39 files, 5206 references checked, 5174 resolved, 32 exempt, 120
skipped as not repo-relative, 0 outside the repository, **0 missing**. With this
report in scope the same scan reads 40 / 5220 / 5188 / 32 / 122, still 0
missing; the changelog row publishes those figures.

## What changed since hyg-audit-04

**Nothing changed that a scanner can see, and this time that is not an
inference — it is a byte-for-byte measurement of the scanner's own output.**

The tree this audit scanned is byte-identical to the tree `hyg-audit-04`
scanned: `git diff 7978f14 origin/main` is empty, and both scans read 766
tracked files, because that tick staged its own two reports before its final
run. On that tree — `origin/main`, before this report existed — regenerating
the evidence file and diffing it against the committed
[audit-2026-09-02-2-evidence.md](audit-2026-09-02-2-evidence.md) returns
**one changed line out of 1,356**: line 3, the banner naming the output path
the generator was invoked with. Every finding row, every never-delete row and
every count is identical, character for character. The scoping matters and is
stated because the measurement does not hold on the tree this report lands in:
once this file exists the same diff returns four changed lines — the banner,
the tracked-file count 766 → 767, and two never-delete rows for this report.
Reproduce the base-tree figure with:

    git worktree add /tmp/base origin/main --detach
    ( cd /tmp/base && node scripts/repo-refs.mjs --evidence /tmp/e.md )
    diff docs/hygiene/audit-2026-09-02-2-evidence.md /tmp/e.md
    git worktree remove /tmp/base --force

**Findings: 20 → 20.** The same twenty, all baselined, none stale.

**Unreferenced exports: 37 → 37**, the same thirty-seven, none in and none out.
An unreferenced export is not a `--check` finding and needs no baseline row.

**Newly unreferenced files: one — this report.** It is on the never-delete
list, so no baseline row was added. That is the same mechanism the last two
audits recorded for their own reports.

**Canon movements: none.** The two transitional `paths-exempt` groups remain
the ones `hyg-audit-03` left (`dcl-07`, `hyg-09`).

### The evidence file this audit did not commit

The four previous audits each committed an evidence file beside their report,
130 KB to 153 KB apiece. This one did not, and the reason is the base-tree
measurement above: the file it would have committed is byte-identical to the
one already on `main` except for a line naming itself, plus the two rows and
the count bump that this report's own existence adds. The item's `files:` list is
`docs/hygiene/audit-<date>.md, tests/fixtures/repo-refs-baseline.json,
spec.md` — the evidence file is not on it — and criterion 1 says the report is
generated *from* the `--evidence` run and that the tick "writes nothing else
except baseline rows and spec.md". The command was run; its output was
compared; the comparison is reported here with the commands that reproduce it. Nothing is lost: the committed evidence for these twenty findings is
`audit-2026-09-02-2-evidence.md`, unchanged and linked above.

This is a deviation from four ticks of precedent, so it is raised as
question 75 rather than made silently, and it is reversible in one command if
the owner rules the other way.

## Cadence

Changelog rows since the `hyg-audit-04` row: **0** — that row is the last line
of the §15 table on `origin/main`, and this branch adds the next one. The
twenty-row half of the cadence is **not** satisfied. Neither is the other half
(*and no item is in review*), since `hyg-05`'s PR #27 has been open since
2026-09-01. The audit is therefore **not DUE on either half**, the same state
`hyg-audit-04` recorded. It ran because it was the next item in the ORDINARY
pick order, which is the acceptance criterion's own no-jump case, and the
commit message states it the same way.

## The two non-restatements: a stale item status, and a propagating citation

The last two audits each raised new questions — 72 and 73 from `hyg-audit-03`,
74 from `hyg-audit-04` — but all three were about the audit's own bookkeeping
or the loop's cadence, and every scanner number they published was a
restatement. This tick found two defects that are not: a stale status on
another item's block, and a rule citation that three changelog rows assert and
no question supports. Both are in `spec.md`'s own bookkeeping rather than in
the tree the scanners walk, which is why neither scanner could have caught
either.

**`fed-06` reads `status: blocked` / `date: 2026-08-29` while its work is
landed on `main`.** All five paths in its `files:` line — `tests/setup/silos.ts`,
`tests/fixtures/silos/`, `tests/federation-e2e.test.ts`,
`tests/routing-recall.test.ts` and `scripts/routing-recall.mjs` — are tracked on
`origin/main` and green in the suite, and §15 carries two rows dated
2026-08-30 recording the landing and its CI hotfix. §0.3 requires a `blocked`
item to carry a dated question; `fed-06`'s is the question numbered 51 dated
2026-08-29 (§14 carries two questions numbered 51 — see the second finding
below), which sets out a chain those two rows record as completed. The likely mechanism is in the landing row itself: that
merge's only conflict was `spec.md` and the resolution kept both changelog
tails, which preserves the §15 rows and drops the item block's status flip.
Raised as question 77; this tick did not flip it, because `fed-06`'s block is
another item's record and this item's own criterion 2 confines the tick to
numbered questions.

**The same check applied to every other non-`done` item, so the finding is
bounded rather than anecdotal.** `ext-05` is genuinely blocked — `SHARED_FACT`
appears nowhere in `src/`, only in a `tests/kb-facts-ingest.test.ts` assertion
that it is absent, and question 59 records a live contradiction awaiting the
owner. `dcl-07` is genuinely blocked per question 71. `hyg-05`, `hyg-06`,
`hyg-07` and `hyg-09` are genuinely unlanded: `src/components/legacy/` still
holds all seven components, `src/lib/utils.ts` still exports `formatDate`,
`formatDateTime` and `timeAgo`, and neither the renamed components directory
hyg-06 would create nor the dockerignore test hyg-07 would add exists on `main`
(their paths are deliberately not written here as code spans: the dead-path
scanner treats a cited path as a claim that it resolves, and these do not yet).
`dcl-08` is genuinely unlanded too — `docling` appears nowhere in
`THIRD_PARTY.md` and the OCR-copy test it would add is not tracked. That is all
eight non-`done` items besides this one, and `fed-06` is the only stale one.

**The second finding: two citation defects in §14, one of which has been
propagating through changelog rows.** Three previous rows state that
question 55 "classes [flipping an item's status on `main`] as a §0 protocol
edit" and that question 34 "forbids the loop making one". Neither says that.
Question 55 is a structural observation that the §0.6 one-review-slot check
cannot be made from `spec.md`, because a Tier-C item's status flip rides in the
work commit on its own branch; it classes nothing. Question 34 reserves exactly
three named prose edits to the owner — an eighth entry on §0.6's Tier-C list,
`.dockerignore` in §0.6 rule 6, and one sentence in §0.2 step 4 — and an item
status flip is none of them. The restraint those rows describe was right; the
authority they cite for it is invented, and it has been copied forward
unchecked. Separately, §14 contains **two questions numbered 51** (2026-08-29,
`fed-06`; 2026-08-30, the `safeFetch` redirect), so every citation of "q51" is
ambiguous — including this audit's own central one, which is why it is dated
above. Raised as question 78. Found by this tick's independent verification
pass, not by either scanner: neither can read a citation.

## Open questions for the owner

Four raised this tick; questions 72, 73 and 74 are unchanged and still stand.
See §14 of `spec.md`:

- **Question 75 — the audit's evidence file is now pure duplication, and this
  tick stopped committing it.** Measured, not argued: regenerated on the base
  tree, the evidence file differs from the committed one in a single line,
  which names the file itself. *Keep assumption: the report is committed and the evidence file is
  not, and the report carries the reproduction command instead. Reverse it by
  saying so here and the next audit will commit one again.*

- **Question 76 — four consecutive ticks have now shipped no source change at
  all, and the last three have left the scanner findings identical at 20.** The
  four are `dcl-07` (blocked, `spec.md` only, 2026-09-01), `hyg-audit-03`,
  `hyg-audit-04` and this one, each a docs-and-spec diff. This is question 74's
  cycle, one turn further on and with the cost now measured rather than
  estimated. Every item ahead of the audit in §11 is in review
  (`hyg-05`), dependency-blocked on a review item (`hyg-06`), Tier C behind the
  one-review-slot rule (`hyg-07`, `hyg-09`), dependent on a blocked item
  (`dcl-08`), or blocked in its own right (`ext-05` and `dcl-07`, plus
  `fed-06`, whose `blocked` is stale per question 77 — none of the three
  selectable by §0.2 step 4 either way). *Keep assumption: the loop keeps reaching
  the re-armed audit every tick and changing nothing. The one action that
  dissolves it with no protocol edit is merging PR #27 — its `mergeable_state`
  was `dirty` when `hyg-audit-04` looked, so it needs a conflict resolution
  first, and §0.6 forbids the loop doing either. The loop APPLIED no fix and
  made no protocol edit.*

- **Question 77 — `fed-06` is marked `blocked` on `main` although it landed on
  2026-08-30 and every file it delivers is tracked.** Its blocking question —
  the one numbered 51 dated 2026-08-29 — sets out a chain that §15's two rows
  dated 2026-08-30 record as completed. *Keep assumption: the loop changed
  nothing. Flipping another item's block is outside criterion 2, which confines
  this tick to numbered questions. Nothing in the pick order moves: `blocked`
  and `done` are both unselectable by §0.2 step 4 and no `todo` item depends on
  `fed-06`.*

- **Question 78 — two citation defects in §14, one of them propagating.**
  Three changelog rows attribute to questions 55 and 34 a rule neither states,
  and §14 numbers two different questions 51. *Keep assumption: nothing was
  renumbered and no row was edited — §15 is append-only and §14 is the owner's.*

## What this tick did not do

Deleted nothing. Moved nothing. Removed no baseline row and edited none. Added
no baseline row — nothing qualified. Appended no backlog item except the
re-arm the acceptance requires. Made no edit to the tick protocol in §0, and
flipped no item's status but its own — neither `hyg-05`'s nor `fed-06`'s.
Another item's block is not this tick's to edit under criterion 2, which
confines it to numbered questions; the stronger claim earlier rows made for
that restraint is itself corrected above.
